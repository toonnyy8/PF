const GPU = require('./src');
const gpu = new GPU();
const kernel = gpu.createKernel(function(input) {
  return Math.random() * input[this.thread.y][this.thread.x];
}, { output: [1] });
kernel.build([1]);